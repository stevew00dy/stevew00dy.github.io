{
  "name": "undisputed-noobs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "apps/landing"
  ],
  "scripts": {
    "dev:landing": "npm run dev -w apps/landing",
    "build": "node scripts/build.mjs",
    "serve:dist": "py -m http.server 4181 --bind 127.0.0.1 -d dist",
    "review:local": "npm run build && npm run serve:dist",
    "deploy": "npm run build && gh-pages -d dist -r https://github.com/stevew00dy/stevew00dy.github.io.git",
    "composite:basic-training": "node scripts/composite-basic-training.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sharp": "^0.34.5"
  }
}
